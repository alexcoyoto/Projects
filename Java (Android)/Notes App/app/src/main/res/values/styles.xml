<resources>

    <style name="CustomAppTheme" parent="@android:style/Theme.Material">
        <item name="android:background">@color/darkGrey</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CustomListView" parent="@android:style/Widget.ListView">
        <item name="android:background">@color/black</item>
    </style>

    <style name="CustomVerticalLinearLayout" >
        <item name="android:background">@color/black</item>
        <item name="android:paddingLeft">5dp</item>
        <item name="android:paddingRight">5dp</item>
        <item name="android:paddingTop">7dp</item>
        <item name="android:paddingBottom">7dp</item>
        <item name="android:orientation">vertical</item>
    </style>

    <style name="CustomHorizontalLinearLayout" >
        <item name="android:background">@color/black</item>
        <item name="android:paddingLeft">7dp</item>
        <item name="android:paddingRight">7dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingBottom">5dp</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:dividerPadding">5dp</item>
    </style>

    <style name="materialToast">
        <item name="stColorBackground">@color/black</item>
        <item name="stSolidBackground">true</item>
        <item name="stTextBold">true</item>
        <item name="stStrokeWidth">3dp</item>
    </style>

    <style name="MainButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/graybutton</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="android:layout_width">100dp</item>
        <item name="android:layout_height">50dp</item>
    </style>

</resources>
